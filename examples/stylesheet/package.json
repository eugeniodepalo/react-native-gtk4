{
  "name": "stylesheet-example",
  "version": "1.0.0",
  "description": "A simple example of a React Native GTK app using stylesheets",
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "build": "tsc",
    "start": "pnpm run build && cp -r data dist/ && node dist/index.js"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-native-gtk4": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.12.10",
    "@types/react": "^18.3.1",
    "typescript": "^5.4.5"
  }
}
