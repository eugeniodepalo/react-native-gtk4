{
  "name": "react-native-gtk",
  "private": true,
  "packageManager": "pnpm@9.6.0",
  "workspaces": [
    "packages/*",
    "packages/ts-for-gir/*",
    "examples/*"
  ],
  "scripts": {
    "build": "cd packages/react-native-gtk4 && turbo build",
    "test": "cd packages/react-native-gtk4 && turbo test",
    "codegen": "cd packages/react-native-gtk4 && turbo codegen",
    "publish": "cd packages/react-native-gtk4 && pnpm publish"
  },
  "devDependencies": {
    "turbo": "^2.0.9"
  }
}
