{
  "name": "react-native-gtk",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "pnpm run --filter react-native-gtk build",
    "test": "pnpm run --filter react-native-gtk test"
  }
}
