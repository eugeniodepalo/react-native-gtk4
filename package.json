{
  "name": "react-native-gtk",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "pnpm --filter react-native-gtk4 run build",
    "test": "pnpm --filter react-native-gtk4 run test",
    "publish": "pnpm --filter react-native-gtk4 publish"
  }
}
