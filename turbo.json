{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["NODE_ENV"],
  "globalPassThroughEnv": [
    "DISPLAY",
    "XDG_RUNTIME_DIR",
    "PULSE_SERVER",
    "DBUS_SESSION_BUS_ADDRESS"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "start": {
      "dependsOn": ["build"],
      "persistent": true,
      "cache": false
    },
    "test": {
      "dependsOn": ["build"],
      "cache": false
    },
    "react-native-gtk4#codegen": {
      "dependsOn": ["codegen#build"],
      "outputs": ["src/generated/**", "__tests__/generated/**"],
      "inputs": ["$TURBO_DEFAULT$", "../../Dockerfile"]
    }
  }
}
